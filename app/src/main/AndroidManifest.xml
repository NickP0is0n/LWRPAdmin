<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="live.nickp0is0n.lwrpadmin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.IpInfoActivity"></activity>
        <activity android:name=".ui.GangMapActivity" />
        <activity android:name=".ui.AdminListActivity" />
        <activity android:name=".ui.AdminPasswordChangeActivity" />
        <activity android:name=".ui.LeaderInfoActivity" />
        <activity android:name=".ui.LeaderListActivity" />
        <activity android:name=".ui.MenuActivity" />
        <activity android:name=".ui.StatsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>